# 기사콜 작업일지

- 날짜: 2026-07-04
- 계정: donglee1228

## 목표
배달/물류 기사용 초간단 모바일 웹앱 "기사콜" 신규 제작 + GitHub Pages 배포.

## 진행
1. 작업 폴더 생성: `C:\Users\dldms\OneDrive\Desktop\기사콜`
2. `index.html` 작성 (단일 파일 자족형, CSS/JS 인라인)
   - 화면1: 남자/여자 성별 선택 (큰 터치 버튼 2개, 남=골드/블루, 여=딥레드/보라)
   - 화면2: "기사님 오늘 물량 15카트넘어요!" 버튼
   - 버튼 클릭 시 사이렌 알림 오버레이(🚨 물량 폭주) 연출만
   - localStorage 등 누적 저장 미사용
   - 다크 마스큘린 테마(#1c1f26 차콜 + #c9a227 골드 + 딥레드) + SVG noise 텍스처
   - 모바일 반응형(viewport, 100dvh, max-width 440px)
3. `README.md`(한국어) 작성
4. git init → commit → GitHub public repo(gisacall) 생성 → push
5. GitHub Pages 활성화(main/root)
6. 라이브 URL 접속 검증

## 파일명 규칙
- 진입점만 예외로 `index.html`, 나머지 한국어(README.md는 GitHub 관례상 영문 유지, 작업일지.md는 한국어)

## 검증 결과 (이중검증)

### 기술 검증 — PASS
- 성별선택 버튼/함수 참조 3건(남자·여자 버튼 2 + 함수 정의 1), `>남자<` `>여자<` 라벨 존재
- 물량 버튼 문구 정확히 존재: "기사님 오늘 물량 15카트넘어요!" (span으로 15카트 강조, 렌더 텍스트 일치)
- localStorage/sessionStorage/indexedDB grep 0건 → 누적 저장 미사용 확인
- HTML 문법 오류 없음, 단일 파일 자족형(외부 의존성 0)

### 요구사항 검증 — PASS
1. 열면 남/여 성별 선택 화면 → 충족
2. 성별 선택 시 물량 버튼 화면 전환 → 충족
3. 물량 버튼 누르면 🚨 사이렌 알림 오버레이 반응 → 충족
4. 누적 저장 없음(새로고침 시 초기화) → 충족

### 라이브 URL 검증 — PASS
- https://donglee1228.github.io/gisacall/ → HTTP 200 (Pages 전파 후 2번째 시도 성공)
- WebFetch로 성별 선택 화면(남자/여자) + 물량 문구 존재 확인

## 산출물
- 저장소: https://github.com/donglee1228/gisacall (public)
- 라이브: https://donglee1228.github.io/gisacall/

---

# 2차 수정 (2026-07-04)

## 변경 내용
1. 상단 "기사콜" 제목 + 모든 안내 문구(brand/label/greet) 삭제 → 깔끔한 화면
2. 성별 버튼 문구 변경: "저는 남자 입니다", "저는 여자입니다" (세로 배치)
3. 선택 후 패널 문구: "기사님 오늘 15카트넘어요!" ("물량" 제거)
4. 패널 클릭 → 🚨 알림 오버레이
5. 알림에 감사 멘트 추가: "감사합니다 오늘 빨리가겠습니다!"
- 다크 마스큘린 테마·누적저장 없음·index.html 진입점 유지, 사용자메모 계속 .gitignore 제외

## 이중검증 결과 (2차)
### 기술 검증 — PASS
- "저는 남자 입니다" 1건, "저는 여자입니다" 1건 존재
- 패널 렌더 텍스트 "기사님 오늘 15카트넘어요!" 정확히 존재("물량" 없음)
- "감사합니다 오늘 빨리가겠습니다!" 존재
- 옛 제목 `기사콜</h1>`·설명 `배달·물류 기사 전용` 삭제 확인
- localStorage/sessionStorage/indexedDB grep 0건, 문법 오류 없음

### 요구사항 검증 — PASS
1. 열면 "저는 남자 입니다 / 저는 여자입니다" 선택 → 충족
2. 선택 후 "기사님 오늘 15카트넘어요!" 패널 → 충족
3. 패널 누르면 알림 표시 → 충족
4. 알림에 감사 멘트 "감사합니다 오늘 빨리가겠습니다!" → 충족
5. 제목/설명 삭제로 깔끔 → 충족

### 라이브 검증 — PASS
- https://donglee1228.github.io/gisacall/ 전파 후 curl로 4개 신규 문구 전부 OK, 옛 제목/설명 삭제 확인
- (참고) WebFetch는 15분 캐시로 옛 버전 반환 → 라이브 서버 직접 grep으로 최종 확인

---

# 3차 수정 (2026-07-04) — 어르신용 종료하기 버튼

## 변경 내용
- 감사 멘트("감사합니다 오늘 빨리가겠습니다!") 화면 아래에 큼직한 "종료하기" 버튼 추가
  - 어르신 배려: 글씨 30px, 패딩 32px 넉넉한 터치영역, 딥레드 톤(다크 마스큘린 유지)
- 동작: window.open('','_self')+window.close() 시도 → 안 닫히면 0.25초 후 폴백
  - 폴백: 전체화면 어두운 오버레이 + 초대형 글씨 "앱을 종료했습니다. 창을 닫아주세요." + "오늘도 안전운행 하세요, 기사님!"
- 누적저장 없음·index.html 진입점·사용자메모 .gitignore 제외 유지

## 이중검증 결과 (3차)
### 기술 검증 — PASS
- ">종료하기<" 버튼 1건, onclick="앱종료()"+function 앱종료 배선 확인
- window.close() 2회 호출, 폴백 안내 "앱을 종료했습니다.<br>창을 닫아주세요." 존재
- 배치 순서: 감사멘트(186) → 종료하기 버튼(188) 아래 위치 확인
- localStorage 0건, 문법 오류 없음

### 요구사항 검증 — PASS
1. 감사멘트 아래 종료하기 버튼 → 충족
2. 큼직/누르기 쉬움(글씨 크게·터치영역 넉넉) → 충족
3. 누르면 window.close 시도 → 충족
4. 안 닫히면 큰 글씨 종료 안내 폴백 → 충족

### 라이브 검증 — PASS
- 전파 후 curl로 ">종료하기<", window.close(), 폴백 안내문구 전부 확인

---

# 4차 수정 (2026-07-04) — 기사 전화하기 목록

## 변경 내용
- 종료하기 버튼 아래 "📞 기사님께 전화하기" 목록 추가 (기사 2명)
  1) 이기동 기사 (뚱땡이) — 010-3805-9275 → tel:01038059275
  2) 김종대 기사 — 010-3383-5808 → tel:01033835808
- 각 카드: 이름(별명 포함)+번호 표시 + 큼직한 "전화하기" 버튼(어르신용 글씨 26px·패딩 24px)
- 전화하기 버튼은 그린 계열(#2e7d5b)로 종료 딥레드와 구분, 골드 테두리로 다크 마스큘린 유지
- `<a href="tel:...">` 링크라 폰에서 누르면 다이얼러 바로 연결
- 누적저장 없음·index.html 진입점·사용자메모 .gitignore 제외 유지

## 이중검증 결과 (4차)
### 기술 검증 — PASS
- href="tel:01038059275" 1건, href="tel:01033835808" 1건 존재
- 이름/번호 정확: "이기동 기사 (뚱땡이)"·010-3805-9275·"김종대 기사"·010-3383-5808
- 전화하기 버튼 2개, 종료하기(215줄) 아래 배치(이기동 221·김종대 226) 확인
- localStorage 0건, 문법 오류 없음

### 요구사항 검증 — PASS
1. 종료하기 아래 기사 전화번호 목록 → 충족
2. 기사 2명 이름(뚱땡이 별명 포함)+번호 표시 → 충족
3. tel: 링크 전화하기 버튼(폰 다이얼러 연결) → 충족
4. 큼직한 버튼·그린 계열로 종료 딥레드와 구분 → 충족

### 라이브 검증 — PASS
- 전파 후 curl로 tel:01038059275·tel:01033835808·이기동·김종대 전부 확인

---

# 5차 수정 (2026-07-04) — 전화 카드 좌우 가로 배치

## 변경 내용
- 세로로 쌓이던 기사 카드 2개를 좌우 나란히(가로 2열) 배치
  - 두 카드를 .driverRow(flex-direction:row, gap 12px)로 감싸고 각 카드 flex:1 1 0(약 50%)
  - 글씨/패딩 축소: 이름 16px·번호 16px, telBtn 20px·패딩 축소하되 min-height 60px로 어르신 터치영역 확보
  - 좁은 폰에서도 한 줄에 2개 들어오게(min-width:0, 카드 text-align center, "전화" 라벨로 단축)
- 성별선택·15카트 패널·감사멘트·종료하기·tel 링크·번호·이름 전부 그대로 유지
- 누적저장 없음·index.html 진입점·다크 마스큘린·사용자메모 .gitignore 제외 유지

## 이중검증 결과 (5차)
### 기술 검증 — PASS
- flex-direction:row 적용(.driverRow), driverRow HTML/CSS 존재
- tel:01038059275·tel:01033835808 링크 유지, 이름/번호(이기동·010-3805-9275·김종대·010-3383-5808) 유지
- localStorage 0건, 문법 오류 없음

### 요구사항 검증 — PASS
1. 두 기사 카드 좌우 나란히(가로 2열) → 충족
2. 크기 축소(글씨·패딩)로 한 줄에 2개 → 충족
3. min-height 60px 등 터치영역 유지(너무 작지 않음) → 충족
4. 나머지 요소·tel/번호/이름 그대로 → 충족

### 라이브 검증 — PASS
- 전파 후 curl로 class="driverRow"·flex-direction:row·두 tel 링크 확인

---

# 6차 수정 (2026-07-04) — PWA화 + 아이콘/에셋 디자인

## 변경 내용
### PWA (홈 화면 추가 시 전체화면 앱)
- `앱설정.webmanifest` 작성: name/short_name="기사콜", display:standalone, orientation:portrait,
  background_color/theme_color=#1c1f26, start_url="./", scope="./", icons 192/512/512(maskable)
- index.html head에 `<link rel="manifest" href="앱설정.webmanifest">` +
  apple-mobile-web-app-capable(yes)·apple-mobile-web-app-status-bar-style(black-translucent)·
  apple-touch-icon(180)·mobile-web-app-capable·apple-mobile-web-app-title·theme-color 유지
- safe-area-inset 상/좌/우/하 padding 처리(노치 대응), viewport-fit=cover 유지
- ※ 홈 화면에 추가해 실행할 때만 주소창이 사라져 전체화면 앱처럼 뜸(정상). README에 안내 추가

### 아이콘/에셋 디자인 (Pillow 생성)
- 아이콘_192.png·아이콘_512.png·아이콘_180.png(apple-touch) 생성
  - 다크 차콜 라운드 배경(수직 그라데이션)+골드 내부 테두리 라인+상단 은은한 글로우
  - 골드 트럭(카고+캡+창+딥레드 휠) + 딥레드 원형 콜 배지 안에 골드 전화 수화기(크레센트 기법)
  - 초안의 수화기가 스마일로 보이는 문제 → 크레센트-카브+대각 회전으로 교정, 실사 확인
- 화면 UI: SVG noise 텍스처 강화(opacity .05→.07)
- 기존 기능/문구/흐름/전화번호 전부 그대로 보존(성별선택·15카트·감사멘트·종료하기·이기동/김종대 전화)

## 이중검증 결과 (6차)
### 기술 검증 — PASS
- HTML: rel="manifest", apple-mobile-web-app-capable, theme-color, 아이콘_180/192/512 링크 존재
- 파일 생성: 아이콘_192(14KB)·512(44KB)·180(13KB)·앱설정.webmanifest(JSON 유효, 3 icons)
- localStorage 0건, 기존 기능 문구 전부 grep 확인
- 라이브 200: manifest·아이콘192·512·180 전부 HTTP 200, 서빙 manifest JSON 파싱 OK(standalone)

### 요구사항 검증 — PASS
1. PWA standalone+apple meta+safe-area → 홈추가 시 주소창 없는 전체화면 조건 충족
2. 트럭+콜 모티프 다크 마스큘린 아이콘 192/512/180 실제 생성 → 충족
3. UI 텍스처 강화(고급 다크 톤) → 충족
4. 기존 기능/문구/흐름/전화번호 전부 보존 → 충족

### 라이브 검증 — PASS
- 첫 PWA 빌드가 Pages 큐에서 wedge(building 9분+) → 빈 커밋 재트리거로 통과(교훈: Pages wedge시 빈커밋 새 run)
- 재빌드 후 manifest/아이콘 전부 200, HTML PWA meta 반영, driverRow·tel·종료하기 등 기존요소 보존 확인

---

# 7차 수정 (2026-07-04) — 하단 잘림 + 아이콘 폰 미반영 픽스

## 문제 1: 폰에서 전화하기 카드가 반쯤 잘림
- 원인: 알림 오버레이가 flex 중앙정렬만 하고 스크롤 불가 → 히어로(siren 72/h2 38/cart 60/exitBtn 30px)가 너무 커서
  하단 driverRow가 화면 밖으로 밀려 잘림
- 조치:
  - 알림 오버레이를 세로 스크롤 가능하게 변경(.alert overflow-y:auto, .alertCard min-height:100dvh + flex column center,
    상/하 padding에 env(safe-area-inset-top/bottom))
  - 히어로 세로 크기 절반↓: siren 72→38, h2 38→24, cart 60→34, thanks 24→18px, 마진 축소
  - 종료하기 30→24px·패딩 32→18px(터치영역은 유지), callBtn 패딩 34→24px, callList 마진/gap 축소
- 실검증(puppeteer 모바일 에뮬 360x640, DSF2): docSW=360==iw(가로 오버플로 0), docSH=640==ih(세로 오버플로 0),
  둘째 telBtn right=329<360·bottom=559<640 → 두 카드·전화버튼 온전히 표시(스크린샷 육안 확인)

## 문제 2: 폰에서 아이콘/에셋 미반영
- 원인 조사 및 조치:
  - 한글 파일명(아이콘_192.png) URL 인코딩 이슈 소지 → 아이콘만 ASCII로 재생성(icon-192/512/180.png)
  - 기존 아이콘이 투명배경(RGBA)이라 iOS apple-touch-icon에서 안 뜰 수 있음 → 불투명 풀블리드 차콜 배경 RGB로 재생성
    (colorType 2 truecolor 확인, 라운드 투명코너 제거·iOS가 자체 라운딩)
  - manifest icons purpose "any maskable"·정확한 sizes·상대경로, apple-touch-icon 링크 2종(기본+180x180)
  - 캐시 무력화: manifest·모든 아이콘 링크에 ?v=3 버전 쿼리
  - 옛 한글 아이콘 3종 git rm

## 이중검증 결과 (7차)
### 기술 검증 — PASS
- 라이브 HTTP 200: index·icon-192/512/180(?v=3)·앱설정.webmanifest 전부 200
- 라이브 PNG 실헤더: 매직바이트 OK·정확 크기 192/512/180·colorType 2(불투명 RGB)
- 라이브 manifest JSON 유효: display standalone, icons=(192·512, any maskable) 실경로 일치
- 레이아웃 CSS 반영: 100dvh(3)·overflow-y:auto(1)·safe-area-inset-bottom(2)·apple-touch-icon(2)
- 기존요소 전부 보존(성별·15카트·감사멘트·종료하기·driverRow·tel 2종), localStorage 0

### 요구사항 검증 — PASS
1. 히어로 세로 절반 축소 + 스크롤/100dvh/safe-area로 전화카드 안잘림(360x640 실측 오버플로 0) → 충족
2. 아이콘 폰 반영: ASCII·불투명 PNG·purpose maskable·?v=3 캐시버스트·라이브 200/헤더검증 → 충족
3. 기존 기능/문구/흐름/전화번호 전부 보존 → 충족

### 라이브 검증 — PASS
- Pages 빌드 built(4113eee), 상기 라이브 실측 전부 통과

---

# 8차 수정 (2026-07-04) — 기사매니저 학습 후 아이콘/주소창 재수정

## 기사매니저(gisa-manager) 학습 요약
- 위치: C:\Users\dldms\OneDrive\Desktop\기사매니저_작업 (remote: donglee1228/gisa-manager)
- (a) 주소창 없이 앱처럼: manifest.json(ASCII)에 display:standalone + start_url:"." + <meta apple-mobile-web-app-capable=yes> + theme-color.
  ★ 서비스워커는 아예 없음 — manifest+apple meta만으로 홈추가 전체화면 동작. viewport에 user-scalable=no.
- (b) 아이콘: 순수 ASCII 파일명(icon-192.png/icon-512.png), 쿼리스트링 없음, manifest icons purpose 미지정,
  apple-touch-icon href=icon-192.png. 아이콘은 RGBA지만 라운드 사각 본체는 불투명(투명은 코너뿐).
- 핵심 교훈: 우리가 붙였던 apple-touch-icon의 ?v=3 쿼리스트링이 iOS에서 아이콘 미표시 유발 소지 → 순수 파일명이 정답.

## 적용(똑같이 말고 유사하게)
- 아이콘 링크 전부 쿼리스트링 제거·순수 ASCII: apple-touch-icon.png(180)·icon-192.png·icon-512.png·favicon.png(64),
  전부 불투명 RGB(colorType 2) 자체 재생성. favicon(브라우저 탭)도 신규 추가.
- manifest: display standalone, icons any + 512 maskable, start_url/scope "./", id 추가(쿼리 제거).
- 서비스워커(서비스워커.js) 신설·등록: 캐시버전 gisacall-v4(옛 캐시 삭제)·network-first(항상 최신 시도, 오프라인 폴백).
  → 설치형 PWA 요건 확실히 충족(홈추가 시 주소창 사라짐) + 아이콘 캐시 갱신.
- 히어로 이미지: 자체 제작 인라인 SVG(골드 트럭+딥레드 콜 배지+램프)를 성별 화면 상단에 추가 → 브라우저에서 이미지 즉시 확인.
  저작권 안전(직접 제작, 외부 에셋/기사매니저 복제 아님).
- 기존 기능/문구/흐름/전화번호·하단 안잘림 레이아웃 전부 보존.

## 이중검증 결과 (8차)
### 기술 검증 — PASS
- 라이브 HTTP 200: index·icon-192·icon-512·apple-touch-icon·favicon·앱설정.webmanifest·서비스워커.js 전부 200
- 라이브 PNG 실헤더: 4종 매직바이트 OK·정확 크기(192/512/180/64)·colorType 2(불투명)
- 라이브 manifest 유효(standalone, icons any+maskable), SW 내용 정상(install/fetch/캐시버전)
- HTML: manifest·apple-touch-icon·서비스워커 등록 스크립트·히어로 SVG 존재, ?v 쿼리 0
- 기존요소 전부 보존(성별·15카트·감사멘트·종료하기·driverRow·tel 2종)·localStorage 0·100dvh/overflow 레이아웃 유지
- 라이브 URL 모바일 에뮬(puppeteer 360x640): heroVisible=true(281px), swReg=true(서비스워커 실제 등록됨),
  manifest/apple-touch-icon 링크 존재(쿼리 없음), asset(manifest·favicon·icon-192) 200 로드 확인

### 요구사항 검증 — PASS
1. 주소창 최소화: SW+manifest standalone+apple meta로 설치형 요건 충족(홈추가 시 전체화면), 안내 README 명시 → 충족
2. 아이콘 실제 반영: 쿼리 제거 순수 ASCII·불투명·favicon 추가·SW 캐시버전 갱신·라이브 200/헤더검증 → 충족
3. 화면 내 히어로 이미지 표시(자체 SVG) → 충족
4. 기사매니저 학습 기법 반영(무SW→우리는 SW 추가 강화, 순수파일명, apple meta) → 충족
5. 기존 기능/전화번호/하단 안잘림 전부 보존 → 충족

### 라이브 검증 — PASS
- Pages 빌드 built(b28eaea), 상기 라이브 실측·모바일 렌더 전부 통과

---

# 9차 수정 (2026-07-04) — ntfy.sh 실제 푸시 알림

## 목표/구조
- 기사(어르신)는 설정 0, 버튼만 클릭. 사장님 폰에만 ntfy 앱+토픽 구독.
- 기사가 "15카트" 버튼 누르면 → 사장님 폰으로 앱 안 켜도 푸시. 백엔드 없이 정적 Pages에서 ntfy.sh로.

## 토픽
- 고정 상수: `gisacall-donglee-kd15cart-7f3k9x` (소문자/숫자/하이픈, 추측난이)

## 구현
- index.html: NTFY_TOPIC 상수 + 푸시전송() 함수. 물량알림() 맨 앞에서 푸시전송() 호출(기존 알림/감사멘트 흐름은 그대로 아래에 유지).
- fetch POST https://ntfy.sh/<토픽>, 헤더 Title:"GisaCall"(ASCII)·Priority:"urgent"·Tags:"truck", body 한글 UTF-8("🚚 남/여 기사님 오늘 물량 15카트 넘어요! (기사콜)").
  - 성별선택('남'/'여')로 선택성별 저장 → 본문 접두.
  - try/catch + fetch.then/catch로 실패(오프라인)해도 화면 흐름 정상, 성공/실패 console.log.
- ★ 헤더는 ASCII만(브라우저 ByteString 제약)이라 제목은 GisaCall, 한글은 전부 본문. Korean 헤더는 ntfy가 첨부파일로 오인(실측 확인)→ 본문 방식이 정답.
- 접이식 "⚙ 사장님 설정 안내"(bossInfo, 기본 접힘·하단 최소화)로 기사에겐 방해 없이, 사장님 3단계 노출. README에도 3단계.
- 누적 저장 없음 유지(토픽만 코드 상수).

## 이중검증 결과 (9차)
### 기술 검증 — PASS
- 라이브 HTML: 토픽 문자열·ntfy.sh fetch·bossInfo 안내 포함, 성별선택('남'/'여') 배선, JS 문법 OK
- 기존요소 전부 보존(성별·15카트·감사멘트·종료하기·driverRow·tel 2종·heroArt·serviceWorker), localStorage 0
- 자산 200 유지: icon-192/512·apple-touch-icon·favicon·manifest·서비스워커.js 전부 200

### ntfy 실왕복 검증 — PASS (실제 앱 경로)
- puppeteer로 라이브 페이지 실구동: 남자 선택→15카트 클릭 → 콘솔 "ntfy 푸시 전송 성공: 🚚 남 기사님 오늘 물량 15카트 넘어요! (기사콜)"
- 그 클릭으로 발신된 메시지를 구독 폴(ntfy.sh/<토픽>/json?poll=1&since=)에서 실제 수신 확인:
  title=GisaCall, tags=[truck], priority=5(urgent), 본문에 "남 기사님"·"15카트"·"기사콜" 포함, 첨부(attachment) 아님
- 동시에 알림 오버레이 show=true, 감사멘트="감사합니다 오늘 빨리가겠습니다!" → 기존 흐름 무손상

### 요구사항 검증 — PASS
1. 기사 버튼→사장님 폰 푸시(앱 안 켜도) 실왕복 증명 → 충족
2. 기사 설정 0(버튼만), 사장님만 토픽 구독 → 충족
3. try/catch로 오프라인에도 화면 흐름 유지 → 충족
4. 사장님 3단계 안내(화면 접이식+README) → 충족
5. 기존 기능/문구/흐름/전화/레이아웃/PWA/아이콘/SW·다크마스큘린·누적저장X 전부 보존 → 충족

### 라이브 검증 — PASS
- Pages 빌드 built(ff3770d), 상기 실왕복·자산·HTML 전부 통과
